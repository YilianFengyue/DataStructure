def pre(op):
    if op == '(':return 4
    if op == '^': return 3
    if op in ('*', '/', '%'): return 2
    if op in ('+', '-'): return 1
    return 0

def process(input):
    nums=[]
    ops=[]
    while(i<input.length()):
       if(isspace(input[i])):
            
    
ls=[1]
ls.pop()
a=eval("3**2")
print(len(ls))

print(a)